# Core API framework
fastapi
uvicorn

# PDF parsing (local)
pdfplumber
PyPDF2>=3.0
pdf2image
Pillow

# Security: password hashing + JWT token generation
passlib[bcrypt]
pyjwt

# File uploads via FastAPI (multipart/ form-data support)
python-multipart

# Data processing
pandas
openpyxl

# OpenAI for accurate financial data extraction (required for 100% accuracy)
openai>=1.0

# AWS SDK for S3 operations and Textract
boto3>=1.20

# Environment variable management
python-dotenv

# HTTP requests for FX conversion
requests
